import google from "../../assets/google.png"

export const OtherOptions = () => {

    return (
        <div>
            <img src={google} alt="google_option" onClick={handleLogin} />
        </div>
    )
}